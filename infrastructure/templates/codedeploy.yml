version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "${application_task_definition}"
        LoadBalancerInfo:
          ContainerName: "${application_container_name}"
          ContainerPort: 80
Hooks:
  - AfterInstall : "${migration_lambda_arn}"
  - AfterAllowTestTraffic: "${integration_tests_lambda_arn}"
