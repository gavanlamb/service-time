namespace Time.Domain.UnitTests.Validators.Records;

public class CreateRecordValidatorTests
{
    
    /*var context = await Context();
    var validator = new AddActivityValidator(context);
    var activity = new Activity { Name = "Test 1" };
    var command = new AddActivityCommand(activity);

    var result = await validator.TestValidateAsync(command);

    result.ShouldHaveValidationErrorFor(x => x.Activity.Name);*/
}