parameters:
  - name: buildNumber
    displayName: NPM build number
    type: string

steps:
  - template: ./aws/ecr/push.yml@templates
    parameters:
      imageName: api-tests
      repositoryName: time-api-tests
      tag: ${{ parameters.buildNumber }}
