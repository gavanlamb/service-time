parameters:
  - name: buildNumber
    displayName: NPM build number
    type: string

steps:
  - template: ./docker/build.yml@templates
    parameters:
      target: api-tests
      workingDirectory: tests/Time.ApiTests
      arguments: "--build-arg BUILD_NUMBER=${{ parameters.buildNumber }}"
      tag: ${{ parameters.buildNumber }}
