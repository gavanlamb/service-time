name: 3.0$(Rev:.r)
resources:
  repositories:
    - repository: templates
      type: github
      name: intelligent-thought/ops-azure-pipelines-templates
    - repository: expensely-templates
      type: github
      name: expensely/azure-devops-templates
      endpoint: Expensely

pool:
  vmImage: ubuntu-latest

trigger:
  - main

pr: none
